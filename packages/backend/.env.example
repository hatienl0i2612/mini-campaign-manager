PORT=3001
DATABASE_URL=postgres://postgres:postgres@localhost:5433/campaign_manager
JWT_SECRET=change-this-in-production
JWT_EXPIRES_IN=7d
NODE_ENV=development
HOST=0.0.0.0
# APP_MODE: 'api' (routes only) | 'worker' (background jobs only) | 'full' (both)
APP_MODE=full
